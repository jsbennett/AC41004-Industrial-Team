extends layout

block content
	style(type="text/css").
		.overlay {
			position: fixed;
			width: 100%;
			height: 100%;
			left: 0;
			top: 0;
			background: rgba(51,51,51,0.7);
			z-index: 10;
		}
	div(id="map" class="overlay")
	script(src="/javascript/marker.js")
