/*
*
*This file is used for the configuration of the database 
*In order to connect to your database, follow the same format as shown below 
*
*/

let config = {
    database: {
        host: "acapper.duckdns.org",
        user: "WebApplication",
        password: "DatabasePassword123",
        database: "IndustrialProject",
        dateStrings: "date"
    }
};

module.exports = config;
